[pytest]
# 该镜像里 ROS 自带的两个 pytest 插件（launch_testing / launch_ros）与 venv 中的
# 新版 pytest 不兼容（hookimpl 签名过期），会在插件自动加载阶段直接报错导致整个
# pytest 会话 INTERNALERROR；这里显式禁用它们（本包不使用 launch_testing）。
addopts = -p no:launch_testing -p no:launch_ros
